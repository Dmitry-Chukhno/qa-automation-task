# Base URLs for the two sites under test. Copy this file to .env (or export
# these variables in your shell) to point the suite at different environments.
DEMOWEBSHOP_BASE_URL=https://demowebshop.tricentis.com
WEBSCRAPER_BASE_URL=https://webscraper.io
